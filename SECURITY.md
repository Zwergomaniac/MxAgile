# Security

- **Secrets:** Store locally in `.env.mendix`. Never version.
- **Bootstrap:** Trust established via system-specific scripts in `/scripts`.
- **Security Contact:** [TODO: ADD CONTACT]

Use caution when handling elevated access to the Mendix environment.