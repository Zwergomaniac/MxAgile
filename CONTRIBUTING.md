# Contributing

1. **Principles:** Adhere to MxAgile lifecycle phases.
2. **Testing:** Use `./scripts/run-docker-isolated.ps1`.
3. **Source/Projection:** Never modify generated projections directly. Fix sources in `.mxagile/` or `/scripts`.