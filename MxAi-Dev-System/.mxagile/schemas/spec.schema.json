{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "MxAgile Feature Specification",
  "description": "Canonical feature specification grouping one or more requirements into a logical unit of work. Canonical location: specs/SPEC-NNN.yml. Primary key: ID. Consumed by build_artifact_index.py for IMPLEMENTED_BY edges (requirements list).",
  "type": "object",
  "required": ["ID", "title", "description", "requirements"],
  "additionalProperties": false,
  "properties": {
    "ID": {
      "type": "string",
      "pattern": "^SPEC-[0-9]{3,}$",
      "description": "Stable unique identifier. Primary key in the artifact index. Must match filename stem (SPEC-001.yml -> ID: SPEC-001)."
    },
    "title": {
      "type": "string",
      "description": "Short feature title."
    },
    "description": {
      "type": "string",
      "description": "High-level description of the feature, its purpose, and value."
    },
    "requirements": {
      "type": "array",
      "description": "IDs of all Requirement artifacts this spec implements. Creates IMPLEMENTED_BY edges: Requirement -> Spec. At least one requirement must be referenced.",
      "minItems": 1,
      "items": {
        "type": "string",
        "pattern": "^REQ-"
      }
    },
    "status": {
      "type": "string",
      "enum": ["draft", "accepted", "superseded"],
      "default": "draft",
      "description": "draft: authored but not reviewed. accepted: approved, downstream work may begin. superseded: replaced by another spec."
    },
    "behavior": {
      "type": "string",
      "description": "Detailed behavior description consolidating the referenced requirements. Describes what the system does, not how."
    },
    "business_rules": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Specific business rules governing this feature."
    },
    "out_of_scope": {
      "type": "array",
      "items": { "type": "string" },
      "description": "What this feature explicitly does not cover."
    },
    "open_items": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Unresolved DECISION REQUIRED items."
    },
    "migrated_from": {
      "type": "string",
      "description": "Source file path for migrated specs."
    },
    "migration_date": {
      "type": "string",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$"
    }
  }
}
