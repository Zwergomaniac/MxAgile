# Reusable Mendix Modules

This directory contains reusable Mendix modules (`.mpk` files or MDL source) that can be imported into projects.

Place shared modules like a "VehicleDataConnector" or "SalesProcess" module here.